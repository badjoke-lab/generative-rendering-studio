{
  "name": "generative-rendering-studio-workspace",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --filter @grs/web dev",
    "build": "pnpm --filter @grs/web build",
    "test": "vitest run",
    "test:browser": "playwright test",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
